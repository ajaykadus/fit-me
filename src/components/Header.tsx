import React from 'react';

const Header = ({ toggleNav }: any) => {
  return (
    <header className="bg-gray-800 text-white p-6 flex justify-between items-center">
      <div className="flex items-center"></div>
    </header>
  );
};

export default Header;
